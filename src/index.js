import React from 'react';
import ReactDOM from 'react-dom';
import App from './App.js';

ReactDOM.render(
  
  <App />,
  
  document.getElementById('root')   //将组件App挂载到root元素里，root元素在index.html里
);


